# Generic ARM64 (Raspberry Pi) CPU-only overrides
# Extends compose.yaml with ARM64 CPU-only configuration
# Services are defined in profile files (profiles/*.yaml)

version: '3.8'

# This file can be used to add ARM64-specific overrides when needed
# Currently empty as services are defined in profile files

