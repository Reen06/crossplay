# Jetson L4T-specific overrides
# Extends compose.yaml with Jetson-specific configurations
# Services are defined in profile files (profiles/*.yaml)

version: '3.8'

# This file can be used to add Jetson-specific overrides when needed
# GPU support is handled via NVIDIA_VISIBLE_DEVICES environment variable
# Individual services can add Jetson-specific environment variables in their profile files

